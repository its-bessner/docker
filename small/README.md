# Overview
Nur ein Container zum Üben. Keine besonderen Fähigkeiten. 

In der `docker-compose.yml` ist als Service __first__ angegeben.
Daraus und aus dem Ordnernamen ergibt sich beim Build mit 
`docker-compose` der Name des Images.