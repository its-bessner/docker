== HowTo ==

This is a simple linux environment. You can read files with `cat` or write them with `vim`. 

You can even create directories with `mkdir` or change them with `chdir`. 

For example you can go to the root directory by typing `cd /`. And when you
got lost just type `cd `. That will bring you back like a charm.

Make yourself home.
